name: keep-api-warm
on:
  schedule:
    - cron: "*/10 * * * *"  # every 10 minutes
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          curl -fsS https://donezo-api-qzek.onrender.com/ || true
